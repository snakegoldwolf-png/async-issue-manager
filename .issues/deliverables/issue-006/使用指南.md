# Obsidian 同步脚本使用指南

> **版本**: v1.0  
> **创建日期**: 2026-02-26  
> **作者**: Filer

---

## 快速开始

### 1. 同步学习日记

```bash
cd ~/.openclaw/shared/obsidian-vault/scripts

python3 sync_to_obsidian.py \
  --content "今天学习了 Obsidian 集成..." \
  --type diary \
  --agent filer
```

### 2. 同步知识文档

```bash
python3 sync_to_obsidian.py \
  --content "# 文档工程化\n\n内容..." \
  --type knowledge \
  --title "文档工程化最佳实践"
```

### 3. 同步文件

```bash
python3 sync_to_obsidian.py \
  --file /path/to/document.md \
  --type knowledge
```

---

## 内容类型

| 类型 | 说明 | 目标目录 |
|------|------|----------|
| `diary` | 学习日记 | `01-Agent/{agent}/学习日记/` |
| `work` | 工作记录 | `01-Agent/{agent}/工作记录/` |
| `knowledge` | 知识文档 | `02-记忆库/语义记忆/技术/` |
| `methodology` | 方法论 | `02-记忆库/语义记忆/方法论/` |
| `tool` | 工具 | `02-记忆库/语义记忆/工具/` |
| `rule` | 强制规则 | `02-记忆库/强制规则/` |
| `event` | 事件记录 | `02-记忆库/情景记忆/` |
| `task` | 进行中任务 | `03-任务管理/Issue追踪/in-progress/` |
| `task_closed` | 已完成任务 | `03-任务管理/Issue追踪/closed/` |
| `meeting` | 会议记录 | `05-团队协作/会议记录/每日站会/` |
| `decision` | 决策记录 | `05-团队协作/决策记录/` |
| `article` | 文章 | `06-资源库/参考资料/文章/` |
| `tweet` | 推文 | `06-资源库/参考资料/推文/` |
| `code` | 代码片段 | `06-资源库/代码片段/` |

---

## 完整参数

```bash
python3 sync_to_obsidian.py \
  --content "内容"        # 要同步的内容
  --file /path/to/file   # 或：要同步的文件
  --type diary           # 内容类型（必填）
  --agent filer          # Agent 名称
  --title "标题"          # 文档标题
  --filename "name.md"   # 目标文件名
  --tags tag1 tag2       # 标签列表
  --append               # 追加模式
  --list                 # 列出 Vault 结构
```

---

## Agent 列表

支持的 Agent：
- filer, debugger, webby, anna, memo
- muse, prad, haire, xiaohong, hunter, dev

---

## 使用示例

### 示例 1：Filer 写学习日记

```bash
python3 sync_to_obsidian.py \
  --content "# 2026-02-26 学习日记\n\n今天完成了 Obsidian 集成..." \
  --type diary \
  --agent filer \
  --tags 学习 Obsidian
```

### 示例 2：同步会议记录

```bash
python3 sync_to_obsidian.py \
  --content "# 每日站会\n\n## 参会人员\n- Filer\n- Debugger" \
  --type meeting \
  --filename "2026-02-26-standup.md"
```

### 示例 3：同步 Issue

```bash
python3 sync_to_obsidian.py \
  --content "# Issue #6\n\n任务描述..." \
  --type task \
  --filename "Issue-006.md" \
  --tags 任务 P1
```

### 示例 4：追加内容

```bash
python3 sync_to_obsidian.py \
  --content "\n\n## 新增内容\n\n追加的内容..." \
  --type diary \
  --agent filer \
  --append
```

---

## Python API

```python
from sync_to_obsidian import sync_content, sync_diary, sync_issue

# 同步内容
result = sync_content(
    content="# 标题\n\n内容",
    content_type="knowledge",
    agent="filer",
    title="文档标题",
    tags=["标签1", "标签2"]
)

# 同步日记
result = sync_diary(
    agent="filer",
    content="今天的学习内容..."
)

# 同步 Issue
result = sync_issue(
    issue_id=6,
    title="Obsidian 集成",
    content="任务描述...",
    status="in-progress"
)
```

---

## 输出格式

```json
{
  "status": "success",
  "mode": "created",
  "path": "/path/to/file.md",
  "type": "diary",
  "agent": "filer"
}
```

---

## 注意事项

1. **Frontmatter 自动生成**：如果内容不以 `---` 开头，会自动添加
2. **目录自动创建**：目标目录不存在时会自动创建
3. **文件名默认**：不指定时使用当天日期 `YYYY-MM-DD.md`
4. **追加模式**：使用 `--append` 在现有文件末尾追加

---

## Vault 位置

```
~/.openclaw/shared/obsidian-vault/
```

用 Obsidian 打开此目录即可使用！

---

**Happy syncing!** 🗂️📚
