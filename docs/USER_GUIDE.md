# Async Issue Manager - 使用指南

**面向读者**：Haire（HR/项目协调者）和所有 Agent

本指南帮助你快速上手 Async Issue Manager 系统，了解如何管理任务、分配工作、跟踪进度。

---

## 📚 目录

- [系统概述](#系统概述)
- [角色与权限](#角色与权限)
- [常用命令](#常用命令)
- [工作流程](#工作流程)
- [Haire 的工作流程](#haire-的工作流程)
- [Agent 的工作流程](#agent-的工作流程)
- [注意事项](#注意事项)
- [最佳实践](#最佳实践)

---

## 系统概述

### 这是什么？

Async Issue Manager 是一个**轻量级的本地任务管理系统**，专为 AI Agent 团队协作设计。

### 用来做什么？

- ✅ **任务管理**：创建、分配、跟踪、关闭 Issue
- ✅ **进度追踪**：实时记录任务进度，支持多 Agent 协作
- ✅ **交付物管理**：确保每个任务都有可验证的交付成果
- ✅ **权限控制**：只有授权用户可以创建 Issue，Agent 只能更新进度
- ✅ **智能匹配**：根据标签和优先级自动推荐合适的 Agent

### 核心理念

```
任务持久化 + 进度可追溯 + 交付物验证 = 高质量协作
```

---

## 角色与权限

### 1. 授权用户（Creator）

**谁是授权用户？**
- `bro`（用户）
- `loryoncloud`（管理员）
- `admin`

**可以做什么？**
- ✅ 创建 Issue
- ✅ 分配 Issue 给 Agent
- ✅ 关闭 Issue
- ✅ 查看所有 Issue 和进度
- ✅ 管理交付物

**如何设置身份？**
```bash
export OPENCLAW_USER=bro
```

### 2. Haire（HR/项目协调者）

**特殊职责**：
- 📋 **项目规划**：接收用户需求，拆解成具体任务
- 👥 **人员匹配**：根据 Agent 专长和负载分配任务
- 📊 **进度监控**：定期检查任务进度，协调资源
- 🔔 **通知协调**：分配任务后通知 Agent 开始工作
- 🎯 **质量把控**：验收交付物，确保任务完成质量

**Haire 的权限**：
- ✅ 查看所有 Issue 和进度
- ✅ 分配/取消分配 Issue
- ✅ 通过 `sessions_send` 通知 Agent
- ❌ 不能创建或关闭 Issue（需要授权用户操作）

### 3. Agent（执行者）

**可以做什么？**
- ✅ 更新任务进度
- ✅ 提交交付物
- ✅ 查看所有 Issue 和进度
- ❌ 不能创建 Issue
- ❌ 不能分配或关闭 Issue

**Agent 专长示例**：
- `hunter`, `prad`, `anna` → 学习、架构、系统设计
- `dev`, `filer` → 基础设施、迁移
- `debugger`, `webby` → 自动化、通知

---

## 常用命令

### 基础路径

所有命令都在这个目录下执行：
```bash
cd ~/.openclaw/shared/async-issue-manager/scripts
```

### 查询 Issue

```bash
# 查看所有 open 状态的 Issue
python3 manager.py list --status open

# 查看进行中的 Issue
python3 manager.py list --status in-progress

# 查看已关闭的 Issue
python3 manager.py list --status closed

# 按标签过滤
python3 manager.py list --status open --labels learning

# 查看某个 Agent 的任务
python3 manager.py list --assignee hunter

# 查看 Issue 详情
python3 manager.py show 1
```

### 分配任务

```bash
# 将 Issue #1 分配给 hunter
python3 manager.py assign 1 hunter

# 取消分配（改回 open 状态）
python3 manager.py unassign 1
```

### 更新进度

```bash
# Agent 更新进度
python3 sync_progress.py update 1 \
  --progress "已完成 50%，正在编写文档" \
  --agent hunter

# 标记为阻塞状态
python3 sync_progress.py update 1 \
  --progress "需要更多资料，等待回复" \
  --status blocked \
  --agent hunter

# 查看进度记录
python3 sync_progress.py view --issue 1
```

### 提交交付物

```bash
# 添加单个文件
python3 deliverable.py add 1 \
  --file ~/path/to/document.md \
  --description "学习笔记"

# 添加整个目录
python3 deliverable.py add 1 \
  --file ~/path/to/project/ \
  --description "项目代码"

# 查看交付物
python3 deliverable.py list --issue 1
```

### 关闭 Issue

```bash
# 关闭 Issue（需要先提交交付物）
python3 manager.py close 1 \
  --resolution "任务完成，已提交学习笔记和适配方案"
```

### 统计信息

```bash
# 查看统计
python3 manager.py stats

# 同步文件系统（修复不一致）
python3 manager.py sync
```

---

## 工作流程

### 完整流程图

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 用户/bro 提出需求                                         │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. Main Agent 接收需求，转发给 Haire                         │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. Haire 进行项目规划                                        │
│    - 分析需求                                                │
│    - 拆解任务                                                │
│    - 匹配 Agent                                              │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. Haire 请求授权用户创建 Issue                              │
│    （通过 Main Agent 转达）                                  │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. 授权用户创建 Issue                                        │
│    python3 manager.py create --title "..." --body "..."      │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. Haire 分配任务给 Agent                                    │
│    python3 manager.py assign <issue_id> <agent>              │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 7. Haire 通知 Agent 开始工作                                 │
│    sessions_send --label agent:<agent>:main                  │
│                  --message "你有新任务 #<issue_id>"          │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 8. Agent 执行任务并更新进度                                  │
│    python3 sync_progress.py update <issue_id>                │
│            --progress "..." --agent <agent>                  │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 9. Agent 完成任务，提交交付物                                │
│    python3 deliverable.py add <issue_id>                     │
│            --file <path> --description "..."                 │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 10. Agent 通知 Haire 任务完成                                │
│     sessions_send --label agent:haire:main                   │
│                   --message "Issue #<issue_id> 已完成"       │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 11. Haire 验收交付物                                         │
│     python3 deliverable.py list --issue <issue_id>           │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 12. Haire 请求授权用户关闭 Issue                             │
│     （通过 Main Agent 转达）                                 │
└────────────────────┬────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────────┐
│ 13. 授权用户关闭 Issue                                       │
│     python3 manager.py close <issue_id>                      │
│             --resolution "..."                               │
└─────────────────────────────────────────────────────────────┘
```

---

## Haire 的工作流程

### 场景 1：接收新需求并分配任务

**步骤**：

1. **接收需求**（来自 Main Agent）
   ```
   Main: "用户需要学习任务主动回报机制，请规划项目"
   ```

2. **分析需求**
   - 任务类型：学习 + 实现
   - 复杂度：中等
   - 预计时间：2-3 天
   - 需要技能：学习能力、系统设计

3. **匹配 Agent**
   ```bash
   # 查看当前 open Issues
   cd ~/.openclaw/shared/async-issue-manager/scripts
   python3 manager.py list --status open
   
   # 查看推荐匹配
   python3 broadcast.py
   ```
   
   **匹配结果**：
   - Issue #001 标签：`learning`, `automation`, `notification`
   - 推荐 Agent：`hunter`（学习能力强）、`debugger`（自动化专家）
   - 决定：分配给 `hunter`

4. **分配任务**
   ```bash
   python3 manager.py assign 1 hunter
   ```

5. **通知 Agent**
   ```bash
   # 通过 sessions_send 通知 hunter
   sessions_send --label agent:hunter:main \
                 --message "你有新任务 #001：任务主动回报机制 - 学习并实现
   
   优先级：P0
   标签：learning, automation, notification
   
   请查看详情：
   cd ~/.openclaw/shared/async-issue-manager/scripts
   python3 manager.py show 1
   
   开始工作后请及时更新进度。"
   ```

6. **记录分配日志**
   ```bash
   echo "[$(date)] 分配 Issue #001 给 hunter" >> ~/.openclaw/shared/async-issue-manager/logs/dispatch.log
   ```

### 场景 2：监控进度并协调资源

**步骤**：

1. **定期检查进度**（建议每天 1-2 次）
   ```bash
   # 查看所有进行中的任务
   python3 manager.py list --status in-progress
   
   # 查看详细进度
   python3 sync_progress.py view
   
   # 生成进度摘要
   python3 sync_progress.py summary
   ```

2. **识别阻塞任务**
   ```bash
   # 查看阻塞状态的进度记录
   python3 sync_progress.py view | grep "blocked"
   ```

3. **协调资源**
   - 如果 Agent 遇到阻塞，主动联系提供帮助
   - 如果任务超时，评估是否需要调整优先级或增加人手
   - 如果 Agent 负载过重，考虑重新分配任务

4. **更新项目状态**
   - 定期向 Main Agent 汇报整体进度
   - 识别风险并提前预警

### 场景 3：验收交付物并关闭任务

**步骤**：

1. **接收完成通知**（来自 Agent）
   ```
   hunter: "Issue #001 已完成，请验收"
   ```

2. **查看交付物**
   ```bash
   # 列出交付物
   python3 deliverable.py list --issue 1
   
   # 查看 Issue 详情
   python3 manager.py show 1
   ```

3. **验收交付物**
   - 检查是否包含所有要求的内容
   - 验证质量是否符合标准
   - 确认验收标准是否全部满足

4. **请求关闭 Issue**
   ```bash
   # 通过 Main Agent 请求授权用户关闭
   sessions_send --label agent:main:main \
                 --message "Issue #001 已完成验收，请关闭：
   
   交付物：
   - 学习笔记（已审核）
   - 适配方案（已审核）
   - POC 代码（已测试）
   
   建议 resolution：
   '已完成任务主动回报机制的学习和适配，提交了完整的学习笔记、适配方案和 POC 代码。'"
   ```

5. **跟进关闭**
   - 确认 Issue 已关闭
   - 通知 Agent 任务完成
   - 更新项目进度

### 场景 4：处理紧急任务

**步骤**：

1. **识别紧急任务**
   ```bash
   # 查看 P0 优先级的任务
   python3 manager.py list --status open --priority P0
   ```

2. **快速分配**
   - 选择最合适且当前负载较轻的 Agent
   - 如果必要，可以从其他任务调配人手

3. **加急通知**
   ```bash
   sessions_send --label agent:<agent>:main \
                 --message "🚨 紧急任务 #<issue_id>
   
   优先级：P0
   需要立即处理！
   
   详情：..."
   ```

4. **密切跟踪**
   - 每小时检查一次进度
   - 主动询问是否需要帮助
   - 及时向用户汇报进展

---

## Agent 的工作流程

### 场景 1：接收新任务

**步骤**：

1. **收到通知**（来自 Haire）
   ```
   Haire: "你有新任务 #001：任务主动回报机制 - 学习并实现"
   ```

2. **查看任务详情**
   ```bash
   cd ~/.openclaw/shared/async-issue-manager/scripts
   python3 manager.py show 1
   ```

3. **确认接收**
   ```bash
   # 回复 Haire
   sessions_send --label agent:haire:main \
                 --message "收到任务 #001，开始工作"
   ```

4. **开始工作并更新进度**
   ```bash
   python3 sync_progress.py update 1 \
     --progress "开始分析任务，阅读学习资料" \
     --agent hunter
   ```

### 场景 2：执行任务并更新进度

**步骤**：

1. **定期更新进度**（建议每 1-2 小时）
   ```bash
   # 第一次更新
   python3 sync_progress.py update 1 \
     --progress "已阅读推文，理解核心机制：background session + cron 监控 + 主动通知" \
     --agent hunter
   
   # 第二次更新
   python3 sync_progress.py update 1 \
     --progress "完成学习笔记，正在分析我们系统的差距" \
     --agent hunter
   
   # 第三次更新
   python3 sync_progress.py update 1 \
     --progress "完成适配方案，开始编写 POC 代码" \
     --agent hunter
   ```

2. **遇到阻塞时**
   ```bash
   python3 sync_progress.py update 1 \
     --progress "需要了解 OpenClaw 的 cron 系统 API，等待文档" \
     --status blocked \
     --agent hunter
   
   # 同时通知 Haire
   sessions_send --label agent:haire:main \
                 --message "Issue #001 遇到阻塞：需要 cron 系统 API 文档"
   ```

3. **解除阻塞后**
   ```bash
   python3 sync_progress.py update 1 \
     --progress "已获得文档，继续开发 POC" \
     --agent hunter
   ```

### 场景 3：完成任务并提交交付物

**步骤**：

1. **提交交付物**
   ```bash
   # 提交学习笔记
   python3 deliverable.py add 1 \
     --file ~/work/task-auto-report-notes.md \
     --description "任务主动回报机制学习笔记"
   
   # 提交适配方案
   python3 deliverable.py add 1 \
     --file ~/work/adaptation-plan.md \
     --description "OpenClaw 系统适配方案"
   
   # 提交 POC 代码
   python3 deliverable.py add 1 \
     --file ~/work/poc/ \
     --description "POC 原型代码"
   ```

2. **最后一次进度更新**
   ```bash
   python3 sync_progress.py update 1 \
     --progress "任务完成，已提交所有交付物：学习笔记、适配方案、POC 代码" \
     --agent hunter
   ```

3. **通知 Haire**
   ```bash
   sessions_send --label agent:haire:main \
                 --message "Issue #001 已完成，请验收
   
   交付物：
   1. 学习笔记（task-auto-report-notes.md）
   2. 适配方案（adaptation-plan.md）
   3. POC 代码（poc/）
   
   所有验收标准已满足。"
   ```

---

## 注意事项

### ⚠️ 权限限制

1. **Agent 不能创建 Issue**
   - 如果需要创建 Issue，请通过 Haire 或 Main Agent 转达给授权用户

2. **Agent 不能关闭 Issue**
   - 完成任务后通知 Haire，由 Haire 请求授权用户关闭

3. **Haire 不能创建或关闭 Issue**
   - 需要通过 Main Agent 转达给授权用户

### ⚠️ 交付物要求

1. **关闭 Issue 前必须提交交付物**
   - 系统会自动检查，没有交付物无法关闭

2. **交付物要完整**
   - 包含所有相关文件（代码、文档、测试）
   - 添加清晰的描述说明

3. **交付物要可验证**
   - 确保他人可以根据交付物复现结果

### ⚠️ 进度更新

1. **及时更新**
   - P0 任务：每 30 分钟
   - P1 任务：每 1-2 小时
   - P2/P3 任务：每天至少一次

2. **描述具体**
   - 说明做了什么、遇到什么问题、下一步计划
   - 避免模糊描述（如"在做了"）

3. **遇到阻塞立即标记**
   - 使用 `--status blocked`
   - 同时通知 Haire

### ⚠️ 文件系统

1. **不要手动修改 .issues/ 目录**
   - 所有操作都通过命令行工具
   - 手动修改可能导致索引不一致

2. **定期同步**
   - 如果发现不一致，运行 `python3 manager.py sync`

---

## 最佳实践

### ✅ 创建 Issue 时

**好的做法**：
- 标题简洁明确
- 描述包含背景、目标、验收标准
- 设置合适的优先级和标签
- 标签要准确，便于匹配 Agent

**示例**：
```bash
python3 manager.py create \
  --title "任务主动回报机制 - 学习并实现" \
  --body "
目标：实现 AI Agent 完成长任务后主动回报

学习资料：https://x.com/zohanlin/status/...

验收标准：
1. 理解核心机制并输出学习笔记
2. 分析我们现有系统的差距
3. 提出适配方案
4. （可选）实现 POC 原型
" \
  --priority P0 \
  --labels learning automation notification
```

### ✅ 分配任务时

**好的做法**：
- 根据 Agent 专长分配
- 考虑 Agent 当前负载
- 使用 `broadcast.py` 查看推荐匹配
- 分配后立即通知 Agent

**示例**：
```bash
# 1. 查看推荐
python3 broadcast.py

# 2. 分配任务
python3 manager.py assign 1 hunter

# 3. 通知 Agent
sessions_send --label agent:hunter:main \
              --message "你有新任务 #001，详情：python3 manager.py show 1"
```

### ✅ 更新进度时

**好的做法**：
- 具体、可量化、有时间节点
- 说明做了什么、遇到什么问题、下一步计划
- 定期更新，不要等到最后

**示例**：
```bash
python3 sync_progress.py update 1 \
  --progress "已完成学习笔记（耗时 2 小时）：
  - 核心机制：background session + cron 监控 + 主动通知
  - 我们的差距：缺少自动清理 cron 的机制
  - 下一步：编写适配方案（预计 2 小时）
  - 当前进度：50%
  " \
  --agent hunter
```

### ✅ 提交交付物时

**好的做法**：
- 提交完整的交付物（代码、文档、测试）
- 添加清晰的描述说明
- 及时提交，不要拖延

**示例**：
```bash
# 提交多个交付物
python3 deliverable.py add 1 \
  --file ~/work/notes.md \
  --description "学习笔记（包含核心机制分析和对比）"

python3 deliverable.py add 1 \
  --file ~/work/plan.md \
  --description "适配方案（包含实施步骤和时间估算）"

python3 deliverable.py add 1 \
  --file ~/work/poc/ \
  --description "POC 代码（包含测试用例和运行说明）"
```

### ✅ 关闭 Issue 时

**好的做法**：
- 验证交付物完整性
- 填写详细的解决方案说明
- 确认所有验收标准已满足

**示例**：
```bash
python3 manager.py close 1 \
  --resolution "
任务已完成：
1. 学习笔记：已理解核心机制（background session + cron 监控 + 主动通知）
2. 差距分析：识别了 3 个关键差距（详见适配方案）
3. 适配方案：提出了 5 个具体优化建议，优先级排序完成
4. POC 代码：实现了基础原型，通过测试

所有验收标准已满足。
"
```

---

## 快速参考

### 常用命令速查

```bash
# 查看 open Issues
python3 manager.py list --status open

# 查看某个 Agent 的任务
python3 manager.py list --assignee hunter

# 分配任务
python3 manager.py assign 1 hunter

# 取消分配
python3 manager.py unassign 1

# 更新进度
python3 sync_progress.py update 1 --progress "..." --agent hunter

# 提交交付物
python3 deliverable.py add 1 --file ~/path --description "..."

# 查看交付物
python3 deliverable.py list --issue 1

# 关闭 Issue
python3 manager.py close 1 --resolution "..."

# 查看统计
python3 manager.py stats

# 同步文件系统
python3 manager.py sync
```

### Issue 状态流转

```
open → (assign) → in-progress → (close) → closed
       ↑                ↓
       └── (unassign) ──┘
```

### 优先级说明

- **P0**：紧急（系统崩溃、安全漏洞）→ 立即处理
- **P1**：高优先级（重要功能、严重 bug）→ 24 小时内
- **P2**：中优先级（常规功能、优化）→ 1 周内
- **P3**：低优先级（文档、清理、探索）→ 有空时处理

---

## 获取帮助

### 查看命令帮助

```bash
python3 manager.py --help
python3 sync_progress.py --help
python3 deliverable.py --help
```

### 查看详细文档

- **README.md**：系统概述和快速开始
- **GUIDELINE.md**：详细使用指南（更全面）
- **USER_GUIDE.md**：本文档（面向 Agent）

### 联系支持

如有问题，请联系：
- **Main Agent**：通过 `sessions_send --label agent:main:main`
- **Haire**：通过 `sessions_send --label agent:haire:main`

---

**最后更新**：2026-02-25  
**版本**：1.0.0  
**维护者**：dev
